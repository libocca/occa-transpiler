#include <cuda_runtime.h>
extern "C" __global__ void _occa_mykern_0(int aaa, int bbb) {
    {
        int i = (0) + blockIdx.x;
        {
            int j = (0) + threadIdx.x;
            // BODY
        }
    }
}
